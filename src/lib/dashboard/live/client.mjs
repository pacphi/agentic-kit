export const LIVE_JS = `
(function(){
  "use strict";
  // Dashboard-wide session token (ADR-0014): read from the launch URL's #
  // fragment once, persisted to localStorage, stripped from the address bar.
  // Duplicated (not shared) between this script and the main dashboard script
  // below it — they are two separate <script> tags with no shared scope, and
  // re-running this idempotent bootstrap in each is simpler than wiring a
  // cross-script handoff for a same-page load order.
  var DASH_TOKEN_KEY="ak-dash-token";
  var DASH_TOKEN=(function(){
    var m=String(location.hash||"").match(/token=([A-Za-z0-9_-]+)/);
    if(m){try{localStorage.setItem(DASH_TOKEN_KEY,m[1]);}catch(e){}try{history.replaceState(null,"",location.pathname+location.search);}catch(e){}}
    try{return localStorage.getItem(DASH_TOKEN_KEY)||"";}catch(e){return"";}
  })();
  // EventSource cannot set request headers, so its two URLs carry the token as
  // a query param — a narrower exception than a full page load: this is a
  // same-origin background stream, never a top-level navigation, so it never
  // enters browser history and there is no third-party Referer to leak it to.
  function dashSseUrl(u){return DASH_TOKEN?u+(u.indexOf("?")<0?"?":"&")+"token="+encodeURIComponent(DASH_TOKEN):u;}
  var PAUSE_LIMIT=256,MAX_TURNS=500,TRANSCRIPT_COLLAPSE_KEY="ak-dash-transcript-collapsed",TERMINAL={completed:1,failed:1,cancelled:1};
  function storedTranscriptCollapsed(){try{return localStorage.getItem(TRANSCRIPT_COLLAPSE_KEY)==="true";}catch(_){return false;}}
  var state={snapshot:{schemaVersion:2,cursor:null,projects:[],sessions:[]},events:[],project:null,selected:null,node:null,browserLevel:"projects",scope:"live",historyWindow:"14d",historySnapshot:{schemaVersion:2,cursor:null,projects:[],sessions:[]},historyLoading:false,historyError:false,paused:false,pending:[],overflow:false,source:null,resyncing:false,lastAt:0,active:false,connection:{key:"connecting",text:"Connecting to local session telemetry…"},positions:{},pinned:{},seen:{},camera:{x:24,y:24,k:1},cameraEpoch:0,fitFor:null,pointer:null,transcriptCollapsed:storedTranscriptCollapsed(),playback:{mode:"live",events:[],items:[],index:0,playing:false,speed:1,timer:null,startAt:null,endAt:null,truncated:false,gap:false},transcript:{source:null,turns:[],seen:{},query:"",follow:true,unread:0,status:"idle",session:null}};
  function el(id){return document.getElementById(id);}
  function esc(s){return String(s==null?"":s).replace(/[&<>"']/g,function(c){return{"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c];});}
  function short(s,n){s=String(s||"");return s.length>n?s.slice(0,n-1)+"…":s;}
  function title(s){return String(s||"activity").replace(/[._-]+/g," ").replace(/\\b\\w/g,function(c){return c.toUpperCase();});}
  function ago(v){var at=Date.parse(v||""),ms=Date.now()-at;if(!Number.isFinite(ms))return"";var s=Math.max(0,Math.round(ms/1000));if(s<5)return"just now";if(s<60)return s+"s ago";var m=Math.round(s/60);if(m<60)return m+"m ago";var h=Math.round(m/60);if(h<24)return h+"h ago";var d=Math.floor(h/24),rh=h%24;if(d<7)return d+"d "+rh+"h ago";var w=Math.floor(d/7),rd=d%7;if(w<52)return w+"w "+rd+"d ago";var y=Math.floor(w/52),rw=w%52;return y+"y "+rw+"w ago";}
  function hostOf(v){return String(v&&v.host||"internal").toLowerCase();}
  function hostName(v){var h=hostOf(v);return{claude:"Claude Code",codex:"Codex",opencode:"OpenCode",internal:"Internal"}[h]||title(h);}
  function hostIcon(v){var h=hostOf(v),view="0 0 16 16",body;if(h==="claude")body='<path d="M8 1.5v13M1.5 8h13M3.4 3.4l9.2 9.2M12.6 3.4l-9.2 9.2"/>';else if(h==="codex"){view="146 227 268 267";body='<path d="M249.176 323.434V298.276C249.176 296.158 249.971 294.569 251.825 293.509L302.406 264.381C309.29 260.409 317.5 258.555 325.973 258.555C357.75 258.555 377.877 283.185 377.877 309.399C377.877 311.253 377.877 313.371 377.611 315.49L325.178 284.771C322.001 282.919 318.822 282.919 315.645 284.771L249.176 323.434ZM367.283 421.415V361.301C367.283 357.592 365.694 354.945 362.516 353.092L296.048 314.43L317.763 301.982C319.617 300.925 321.206 300.925 323.058 301.982L373.639 331.112C388.205 339.586 398.003 357.592 398.003 375.069C398.003 395.195 386.087 413.733 367.283 421.412V421.415ZM233.553 368.452L211.838 355.742C209.986 354.684 209.19 353.095 209.19 350.975V292.718C209.19 264.383 230.905 242.932 260.301 242.932C271.423 242.932 281.748 246.641 290.49 253.26L238.321 283.449C235.146 285.303 233.555 287.951 233.555 291.659V368.455L233.553 368.452ZM280.292 395.462L249.176 377.985V340.913L280.292 323.436L311.407 340.913V377.985L280.292 395.462ZM300.286 475.968C289.163 475.968 278.837 472.259 270.097 465.64L322.264 435.449C325.441 433.597 327.03 430.949 327.03 427.239V350.445L349.011 363.155C350.865 364.213 351.66 365.802 351.66 367.922V426.179C351.66 454.514 329.679 475.965 300.286 475.965V475.968ZM237.525 416.915L186.944 387.785C172.378 379.31 162.582 361.305 162.582 343.827C162.582 323.436 174.763 305.164 193.563 297.485V357.861C193.563 361.571 195.154 364.217 198.33 366.071L264.535 404.467L242.82 416.915C240.967 417.972 239.377 417.972 237.525 416.915ZM234.614 460.343C204.689 460.343 182.71 437.833 182.71 410.028C182.71 407.91 182.976 405.792 183.238 403.672L235.405 433.863C238.582 435.715 241.763 435.715 244.938 433.863L311.407 395.466V420.622C311.407 422.742 310.612 424.331 308.758 425.389L258.179 454.519C251.293 458.491 243.083 460.343 234.611 460.343H234.614ZM300.286 491.854C332.329 491.854 359.073 469.082 365.167 438.892C394.825 431.211 413.892 403.406 413.892 375.073C413.892 356.535 405.948 338.529 391.648 325.552C392.972 319.991 393.766 314.43 393.766 308.87C393.766 271.003 363.048 242.666 327.562 242.666C320.413 242.666 313.528 243.723 306.644 246.109C294.725 234.457 278.307 227.042 260.301 227.042C228.258 227.042 201.513 249.815 195.42 280.004C165.761 287.685 146.694 315.49 146.694 343.824C146.694 362.362 154.638 380.368 168.938 393.344C167.613 398.906 166.819 404.467 166.819 410.027C166.819 447.894 197.538 476.231 233.024 476.231C240.172 476.231 247.058 475.173 253.943 472.788C265.859 484.441 282.278 491.854 300.286 491.854Z"/>';}else if(h==="opencode"){view="0 0 300 300";body='<g transform="translate(30 0)"><path d="M180 240H60V120H180V240Z" fill="#4B4646"/><path d="M180 60H60V240H180V60ZM240 300H0V0H240V300Z" fill="#F1ECEC" fill-rule="evenodd"/></g>';}else body='<path d="M3 8h10M8 3v10"/>';return'<svg class="live-host-icon" viewBox="'+view+'" focusable="false" aria-hidden="true" data-host="'+esc(h)+'">'+body+'</svg>';}
  function branchIcon(){return'<svg class="live-branch-icon" viewBox="0 0 24 24" focusable="false" aria-hidden="true"><line x1="6" x2="6" y1="3" y2="15"></line><circle cx="18" cy="6" r="3"></circle><circle cx="6" cy="18" r="3"></circle><path d="M18 9a9 9 0 0 1-9 9"></path></svg>';}
  function usefulDirectory(w){var d=String(w&&w.directoryLabel||"").trim();return d&&d!=="repo root"?d:"";}
  function workspaceText(s){var w=s&&s.workspace;if(!w)return"Workspace details not recorded";var parts=[],directory=usefulDirectory(w);if(directory)parts.push("Directory: "+directory);if(w.branchLabel)parts.push("Branch: "+w.branchLabel);return parts.join(" · ")||"Workspace details not recorded";}
  function workspaceMarkup(s){var w=s&&s.workspace;if(!w)return"";var parts=[],directory=usefulDirectory(w);if(directory)parts.push('<span class="live-session-directory">'+esc(directory)+'</span>');if(w.branchLabel)parts.push('<span class="live-session-branch" title="'+esc("Branch: "+w.branchLabel)+'">'+branchIcon()+'<span>'+esc(w.branchLabel)+'</span></span>');return parts.length?'<span class="live-session-workspace" aria-hidden="true">'+parts.join('<span class="live-workspace-separator">·</span>')+'</span>':"";}
  function workspaceDelta(s){var c=s&&s.workspace&&s.workspace.changes;if(!c||!Number.isFinite(c.additions)||!Number.isFinite(c.deletions))return"";return'<span class="live-delta" aria-label="'+esc(c.additions+' lines added, '+c.deletions+' lines deleted; tracked working tree at capture, not attributed to this session')+'"><b>+'+Number(c.additions)+'</b><i>−'+Number(c.deletions)+'</i></span>';}
  function workspaceDescription(s){var w=s&&s.workspace,c=w&&w.changes,parts=["Execution host: "+hostName(s),"Inference provider: "+inferenceProviderName(s)],context=workspaceText(s);if(context!=="Workspace details not recorded")parts.push(context);if(c)parts.push("Working tree at capture: "+c.additions+" lines added, "+c.deletions+" deleted in tracked files; not attributed to this session");if(w&&w.capturedAt)parts.push("Captured "+(ago(w.capturedAt)||w.capturedAt));return parts.join(". ");}
  function inferenceProviderOf(v){if(v&&v.provider)return String(v.provider);var nodes=v&&v.nodes||[],session=nodes.find(function(n){return n.kind==="session"&&n.provider;})||nodes.find(function(n){return n.kind==="subagent"&&n.provider;});return session&&session.provider||null;}
  function inferenceProviderName(v){var p=inferenceProviderOf(v);if(!p)return"Provider not established";return{anthropic:"Anthropic",openai:"OpenAI",openrouter:"OpenRouter",ollama:"Ollama",google:"Google Gemini",bedrock:"AWS Bedrock",vertex:"Google Vertex AI",foundry:"Microsoft Foundry",gateway:"Custom gateway"}[String(p).toLowerCase()]||title(p);}
  function providerEvidence(v){var p=v&&v.providerProvenance;if(!p&&v&&v.nodes){var n=v.nodes.find(function(x){return x.kind==="session"&&x.provider;})||v.nodes.find(function(x){return x.kind==="subagent"&&x.provider;});p=n&&n.providerProvenance;}return p||"unknown";}
  function providerRank(v){return{unknown:0,inferred:1,configured:2,observed:3}[v]||0;}
  function coverageOf(s){if(s&&s.coverage)return s.coverage;var adapters=(s&&s.nodes||[]).map(function(n){return n.sourceAdapter;}).filter(Boolean),detailed=adapters.some(function(a){return a==="claude-transcript"||a==="codex-rollout";});return{presence:presenceState(s)!=="unknown"?"observed":"unavailable",activity:detailed?"events":presenceState(s)!=="unknown"?"presence-only":"unavailable",actors:(s&&s.nodes||[]).some(function(n){return(n.kind==="agent"||n.kind==="subagent")&&n.id!==s.id;})?"embedded-actors":"unavailable",resources:detailed?"lifecycle":"unavailable",hierarchy:"unavailable",transcript:detailed?"session":"unavailable",playback:detailed?"session":"unavailable"};}
  function kindName(k){return{session:"Coordinator",agent:"Agent",subagent:"Worker",tool:"Tool",skill:"Skill",plugin:"Plugin",mcp:"MCP tool",gate:"Quality gate"}[k]||title(k||"agent");}
  function statusText(v){return{running:"Working",queued:"Queued",quiescent:"Waiting",expired:"No recent activity",blocked:"Blocked",completed:"Completed",failed:"Failed",cancelled:"Cancelled",idle:"Waiting"}[v]||"Outcome unavailable";}
  function label(n){return n&&(n.toolName||n.label||n.toolCategory||n.role&&title(n.role)||kindName(n.kind))||"Activity";}
  function actionText(a,n,s){if(a==="tool.started")return label(n)+" started";if(a==="tool.completed")return label(n)+(s==="failed"?" failed":" completed");return{"session.started":"Session started","session.input":"New input","agent.output":"Response produced","agent.spawned":"Worker dispatched",contains:"Worker linked","agent.planned":"Agent scheduled","session.completed":"Session completed","session.failed":"Session failed"}[a]||title(a);}
  function activityContext(n,view){var model=String(n&&n.model||"");if(model&&model!=="unknown"&&!/^<[^>]+>$/.test(model))return model;return ago(n&&n.observedAt)||view.label;}
  function sessionsOf(v){var x=v&&v.snapshot?v.snapshot:v;return x&&Array.isArray(x.sessions)?x:{schemaVersion:2,cursor:null,projects:[],sessions:[]};}
  // Live scope reads the SSE-fed state.snapshot; History scope reads the
  // separately-fetched, date-windowed state.historySnapshot. Kept as two
  // buckets (not one shared state.snapshot) so a live delta arriving while
  // History is on screen can never silently overwrite the historical view.
  function currentSnapshot(){return state.scope==="history"?state.historySnapshot:state.snapshot;}
  function renderConnection(){var history=state.scope==="history",c=history?(state.historyLoading?{key:"connecting",text:"Loading "+state.historyWindow+" of history…"}:state.historyError?{key:"offline",text:"History unavailable · try again"}:{key:"history",text:"History · "+state.historyWindow+" · retained local sessions"}):state.connection;el("live-state-dot").dataset.state=c.key;el("live-state-text").textContent=c.text;el("live-browser-state-dot").dataset.state=c.key;el("live-browser-state").textContent=c.text;}
  function setConnection(k,t){state.connection={key:k,text:t};renderConnection();}
  function sessionKey(s){return String(s&&s.key||(hostOf(s)+":"+(s&&s.id||"")));}
  function presenceState(s){return s&&s.presence&&s.presence.state||"unknown";}
  function hasActiveWork(s){return!!s&&s.lifecycle==="active"&&s.activity&&s.activity.state==="working";}
  function isLiveSession(s){return!!s&&!TERMINAL[s.status]&&(presenceState(s)==="present"||hasActiveWork(s));}
  function sessionState(s){var key=s&&s.status||"unknown",life=s&&s.lifecycle||"historical";if(key==="failed"||key==="cancelled"||key==="completed"||key==="blocked"||key==="queued")return{key:key,label:statusText(key)};if(hasActiveWork(s))return{key:"working",label:"Working now"};if(presenceState(s)==="present")return{key:"present",label:"Process active · quiet"};if(life==="quiescent")return{key:"waiting",label:"Waiting for activity"};if(life==="expired")return{key:"stale",label:"No recent activity"};if(life==="historical"&&key==="running")return{key:"stale",label:"Last seen working"};return{key:"historical",label:"Last active"};}
  function entityState(n,s){var key=n&&n.status||"unknown",resource=n&&({tool:1,skill:1,plugin:1,mcp:1})[n.kind];if(key==="failed"||key==="cancelled"||key==="completed"||key==="blocked"||key==="queued")return{key:key,label:statusText(key)};if(resource&&key==="running")return{key:"working",label:"Running"};if(n&&n.kind==="session"&&presenceState(s)==="present")return hasActiveWork(s)?{key:"working",label:"Working"}:{key:"present",label:"Process active · quiet"};if(key==="running"&&n&&n.sourceAdapter!=="codex-state"&&n.lastAction!=="session.heartbeat")return{key:"working",label:"Working"};if(key==="running")return{key:"stale",label:"Last seen working"};if(key==="quiescent")return{key:"waiting",label:"Waiting"};if(key==="expired")return{key:"stale",label:"No recent activity"};return{key:"historical",label:"Activity recorded"};}
  function historicalView(s){return state.scope==="history"||state.playback.mode==="review"||!isLiveSession(s);}
  function historicalEntityState(n){var key=n&&n.status||"unknown";if(key==="failed"||key==="cancelled"||key==="completed"||key==="blocked")return{key:key,label:statusText(key)};return{key:"historical",label:"Activity recorded"};}
  function displayEntityState(n,s){return historicalView(s)?historicalEntityState(n):entityState(n,s);}
  function displaySessionState(s){var raw=sessionState(s);if(state.scope!=="history")return raw;if(raw.key==="failed"||raw.key==="cancelled"||raw.key==="completed"||raw.key==="blocked")return raw;return{key:"historical",label:"Last active"};}
  function sessionById(id){return currentSnapshot().sessions.find(function(s){return sessionKey(s)===id||s.id===id;});}
  function projectName(s){return s&&s.project&&s.project!=="unknown"?s.project:"Project not reported";}
  function rootSession(s){var seen={};while(s&&s.parentSessionId&&!seen[sessionKey(s)]){seen[sessionKey(s)]=1;var parent=currentSnapshot().sessions.find(function(candidate){return candidate.host===s.host&&candidate.id===s.parentSessionId;});if(!parent)break;s=parent;}return s;}
  function childSessions(root){if(!root)return[];var descendants=[],frontier=[root.id],seen={};seen[sessionKey(root)]=1;while(frontier.length){var parent=frontier.shift();currentSnapshot().sessions.forEach(function(s){if(s.host===root.host&&s.parentSessionId===parent&&!seen[sessionKey(s)]){seen[sessionKey(s)]=1;descendants.push(s);frontier.push(s.id);}});}return descendants.sort(function(a,b){var ar=isLiveSession(a)?0:1,br=isLiveSession(b)?0:1;return ar-br||Date.parse(b.updatedAt||0)-Date.parse(a.updatedAt||0);});}
  function embeddedWorkers(root){if(!root)return[];var childIds={};childSessions(root).forEach(function(s){childIds[s.id]=1;});return(root.nodes||[]).filter(function(n){return(n.kind==="agent"||n.kind==="subagent")&&n.id!==root.id&&!childIds[n.id];}).sort(function(a,b){var aw=entityState(a,root).key==="working"?0:1,bw=entityState(b,root).key==="working"?0:1;return aw-bw||String(label(a)).localeCompare(String(label(b)));});}
  function isNavigationRoot(s){return!!s&&(s.navigationRoot===true||(s.navigationRoot!==false&&rootSession(s)===s));}
  function visibleSessions(){return currentSnapshot().sessions.filter(function(s){return isNavigationRoot(s)&&String(s.projectKey||projectName(s))===state.project&&(state.scope==="live"?isLiveSession(s):!isLiveSession(s));}).sort(function(a,b){return Date.parse(b.updatedAt||0)-Date.parse(a.updatedAt||0);});}
  function receiveSnapshot(v,reset){state.snapshot=sessionsOf(v);state.lastAt=Date.now();if(reset){state.events=[];state.positions={};state.seen={};state.node=null;state.fitFor=null;}var projects=projectCatalog();if(!projects.some(function(p){return p.id===state.project;}))state.project=projects[0]&&projects[0].id||null;if(!sessionById(state.selected)||String(sessionById(state.selected).projectKey||projectName(sessionById(state.selected)))!==state.project){var list=visibleSessions();selectSession(list[0]&&sessionKey(list[0]),true);}render();}
  function applyEvent(ev){
    if(!ev||!ev.sessionId||!ev.actor||!ev.actor.id)return;if(ev.eventId&&state.seen[ev.eventId])return;
    var sessions=state.snapshot.sessions.slice(),evKey=String(ev.sessionKey||ev.host+":"+ev.sessionId),i=sessions.findIndex(function(s){return sessionKey(s)===evKey;});if(ev.action==="session.rebound"){if(!state.resyncing)connect();return;}var s=i>=0?Object.assign({},sessions[i]):{id:ev.sessionId,key:evKey,project:ev.project||"unknown",projectKey:ev.projectKey||null,host:ev.host||"internal",status:"unknown",lifecycle:"historical",nodes:[],edges:[],updatedAt:ev.observedAt},signal=ev.signal||{kind:ev.action==="session.heartbeat"?"presence":(ev.target?"operation":"activity"),phase:ev.status||"observed",correlationId:ev.eventId||null},presenceOnly=signal.kind==="presence";if(ev.project&&ev.project!=="unknown"){s.project=ev.project;s.projectKey=ev.projectKey;}if(ev.workspace){s.workspace=Object.assign({},s.workspace||{},ev.workspace,{changes:ev.workspace.changes||(s.workspace&&s.workspace.changes)||null});}if(ev.parentSessionId)s.parentSessionId=ev.parentSessionId;if(presenceOnly)s.presence={state:ev.status==="quiescent"||ev.status==="expired"?"absent":"present",lastObservedAt:ev.observedAt,evidence:ev.source&&ev.source.adapter||"unknown"};else s.activity={state:ev.status==="running"?"working":(ev.status||"observed"),lastActivityAt:ev.observedAt,currentOperationId:ev.target&&ev.target.id||null,evidence:ev.source&&ev.source.adapter||"unknown"};
    s.nodes=(s.nodes||[]).slice();s.edges=(s.edges||[]).slice();var targetTerminal=!!ev.target&&/\\.(completed|failed|cancelled)$/.test(ev.action||""),priorActor=s.nodes.find(function(n){return n.id===ev.actor.id;});
    function upsert(a,status){var at=s.nodes.findIndex(function(n){return n.id===a.id;}),old=at>=0?s.nodes[at]:{},incomingProvider=a.provider||null,incomingEvidence=ev.providerProvenance||"unknown";if(presenceOnly&&at>=0){if(incomingProvider&&(!old.provider||providerRank(incomingEvidence)>=providerRank(old.providerProvenance))){s.nodes[at]=Object.assign({},old,{provider:incomingProvider,providerProvenance:incomingEvidence,host:a.host||ev.host||old.host,surface:ev.surface||old.surface});}return;}var provider=incomingProvider||old.provider||null,next=Object.assign({},old,{id:a.id,kind:a.kind||old.kind,role:a.role||old.role,provider:provider,providerProvenance:provider?(incomingProvider?incomingEvidence:(old.providerProvenance||incomingEvidence)):"unknown",model:a.model||old.model,label:a.label||old.label,toolName:a.toolName||old.toolName,host:a.host||ev.host||old.host,surface:ev.surface||old.surface,status:status||old.status,observedAt:ev.observedAt,confidence:ev.source&&ev.source.confidence||old.confidence,lastAction:ev.action,lastSignal:signal,durationMs:ev.attributes&&ev.attributes.durationMs||old.durationMs});if(incomingProvider&&old.provider&&providerRank(old.providerProvenance)>providerRank(incomingEvidence)){next.provider=old.provider;next.providerProvenance=old.providerProvenance;}if(TERMINAL[old.status]&&!TERMINAL[next.status])next.status=old.status;if(at>=0)s.nodes[at]=next;else s.nodes.push(next);}
    upsert(ev.actor,targetTerminal?priorActor&&priorActor.status||"running":ev.status);if(ev.target&&ev.target.id){upsert(Object.assign({},ev.target,{label:ev.attributes&&(ev.attributes.toolName||ev.attributes.toolCategory)||ev.target.label,toolName:ev.attributes&&ev.attributes.toolName}),targetTerminal?ev.status:"running");var eid=ev.actor.id+"|"+ev.action+"|"+ev.target.id,edge={id:eid,source:ev.actor.id,target:ev.target.id,action:ev.action,confidence:ev.source&&ev.source.confidence,observedAt:ev.observedAt,status:ev.status,signal:signal},ei=s.edges.findIndex(function(x){return x.id===eid;});if(ei>=0)s.edges[ei]=edge;else s.edges.push(edge);}
    if(!presenceOnly&&ev.actor.kind==="session"&&/^session\\./.test(ev.action||""))s.status=ev.status;if(!presenceOnly&&ev.status==="running"&&(!ev.source||ev.source.adapter!=="codex-state"))s.lifecycle="active";else if(!s.lifecycle)s.lifecycle="historical";s.updatedAt=ev.observedAt;if(i>=0)sessions[i]=s;else sessions.push(s);
    state.snapshot={schemaVersion:state.snapshot.schemaVersion||2,cursor:ev.eventId||state.snapshot.cursor,projects:state.snapshot.projects,sessions:sessions,health:state.snapshot.health};if(ev.eventId)state.seen[ev.eventId]=1;state.events.unshift(ev);state.events=state.events.slice(0,100);state.lastAt=Date.now();if(!state.selected)selectSession(sessionKey(s),false);if(state.playback.mode==="live")render();
  }
  function receive(k,d){if(state.paused){if(state.pending.length<PAUSE_LIMIT)state.pending.push({kind:k,data:d});else{state.pending=[];state.overflow=true;}return;}if(k==="delta"&&!d.snapshot)applyEvent(d&&d.event?d.event:d);else receiveSnapshot(d,k==="reset"||!!(d&&d.reset));}
  function connect(){if(state.resyncing)return;state.resyncing=true;setConnection("connecting","Loading current work…");fetch("/api/live",{cache:"no-store",headers:{"x-dash-token":DASH_TOKEN}}).then(function(r){if(!r.ok)throw Error();return r.json();}).then(function(d){state.resyncing=false;receiveSnapshot(d);openStream();}).catch(function(){state.resyncing=false;setConnection("offline","Live telemetry unavailable.");});}
  function openStream(){if(state.source)state.source.close();if(!window.EventSource)return;var src=new EventSource(dashSseUrl("/api/live/events"));state.source=src;src.onopen=function(){setConnection(state.paused?"paused":"live",state.paused?"Updates paused":"Live · following agent activity");};["init","reset","delta"].forEach(function(k){src.addEventListener(k,function(e){try{receive(k,JSON.parse(e.data));}catch(_){}});});src.onerror=function(){setConnection("offline","Connection interrupted · reconnecting…");};}
  // History scope's own one-shot fetch — deliberately NOT routed through
  // receiveSnapshot()/state.snapshot: that bucket is fed by the live SSE
  // stream (still connected in the background so switching back to Live is
  // instant), and a delta arriving mid-browse must never clobber this view.
  function fetchHistory(){state.historyLoading=true;state.historyError=false;render();fetch("/api/live/history?window="+encodeURIComponent(state.historyWindow),{cache:"no-store",headers:{"x-dash-token":DASH_TOKEN}}).then(function(r){if(!r.ok)throw Error();return r.json();}).then(function(d){state.historyLoading=false;state.historySnapshot=sessionsOf(d);state.lastAt=Date.now();state.events=[];state.positions={};state.seen={};state.node=null;state.fitFor=null;var projects=projectCatalog();if(!projects.some(function(p){return p.id===state.project;}))state.project=projects[0]&&projects[0].id||null;var list=visibleSessions();selectSession(list[0]&&sessionKey(list[0]),true);render();}).catch(function(){state.historyLoading=false;state.historyError=true;render();});}
  function graphData(s){
    var all=s&&s.nodes||[],edges=s&&s.edges||[],isTool={tool:1,skill:1,plugin:1,mcp:1},allTools=all.filter(function(n){return isTool[n.kind];}),nodes=all.filter(function(n){return!isTool[n.kind];}),owner={},history={};
    edges.forEach(function(e){var t=allTools.find(function(n){return n.id===e.target;}),src=allTools.find(function(n){return n.id===e.source;});if(t)owner[t.id]=e.source;if(src)owner[src.id]=e.target;});
    allTools.forEach(function(t){if(!owner[t.id])owner[t.id]=(nodes.find(function(n){return n.kind==="agent"||n.kind==="subagent"||n.kind==="session";})||{}).id;});
    var byOwner={};allTools.forEach(function(t){var o=owner[t.id]||"unowned";(byOwner[o]||(byOwner[o]=[])).push(t);});var tools=[];Object.keys(byOwner).forEach(function(o){var group=byOwner[o].sort(function(a,b){var ar=a.status==="running"?0:1,br=b.status==="running"?0:1;return ar-br||Date.parse(b.observedAt||0)-Date.parse(a.observedAt||0);}),active=group.filter(function(t){return t.status==="running";}),recent=group.filter(function(t){return t.status!=="running";}).slice(0,6);tools=tools.concat(active,recent);history[o]=Math.max(0,group.length-active.length-recent.length);});
    var visible={};nodes.slice(0,80).forEach(function(n){visible[n.id]=1;});return{nodes:nodes.slice(0,80),tools:tools.slice(0,80),owner:owner,history:history,totalTools:allTools.length,edges:edges.filter(function(e){return visible[e.source]&&visible[e.target];})};
  }
  function layout(s,nodes,force,toolGroups){var key=s?sessionKey(s):"none",out=force?{}:state.positions[key]||{},edges=s&&s.edges||[],parents={};edges.forEach(function(e){if(!({tool:1,skill:1,plugin:1,mcp:1})[(nodes.find(function(n){return n.id===e.target;})||{}).kind])parents[e.target]=e.source;});var levels={};function depth(id,seen){if(levels[id]!=null)return levels[id];if(seen[id])return 0;seen[id]=1;var p=parents[id],d=p?1+depth(p,seen):0;return levels[id]=Math.min(d,5);}var groups={};nodes.forEach(function(n){var d=depth(n.id,{});(groups[d]||(groups[d]=[])).push(n);});Object.keys(groups).forEach(function(k){var group=groups[k].slice().sort(function(a,b){return String(a.id).localeCompare(String(b.id));}),column=Number(k),cursor=96;group.forEach(function(n){var count=(toolGroups[n.id]||[]).length,foot=Math.max(220,count*58+84),y=cursor+foot/2;if(!out[n.id])out[n.id]={x:140+column*500,y:y};cursor+=foot+64;});});state.positions[key]=out;return out;}
  function edgePath(a,b){var dx=b.x-a.x,dy=b.y-a.y,bend=Math.min(110,Math.max(45,Math.abs(dx)*.35));return"M"+a.x+" "+a.y+" C"+(a.x+(dx>=0?bend:-bend))+" "+a.y+" "+(b.x-(dx>=0?bend:-bend))+" "+b.y+" "+b.x+" "+b.y;}
  function toolPosition(owner,index,count){var row=index-(count-1)/2;return{x:owner.x+328,y:owner.y+row*58};}
  function kindShape(k){if(k==="session")return'<circle class="node-core node-core-ring" r="18"></circle><circle class="node-core-dot" r="5"></circle>';if(k==="gate")return'<path class="node-core" d="M0-21L19-10V10L0 21-19 10V-10Z"></path><path class="node-core-mark" d="M-7 0l5 5L8-7"></path>';return'<path class="node-core" d="M0-22L19-11V11L0 22-19 11V-11Z"></path><circle class="node-core-dot" r="5"></circle>';}
  function renderGraph(s,force){
    var historyMode=historicalView(s),d=graphData(s),toolGroups={};el("live-canvas").dataset.scope=historyMode?"history":"live";d.tools.forEach(function(t){var o=d.owner[t.id];if(o)(toolGroups[o]||(toolGroups[o]=[])).push(t);});var pos=layout(s,d.nodes,force,toolGroups),selected=state.node,related={};if(selected){related[selected]=1;(s&&s.edges||[]).forEach(function(e){if(e.source===selected)related[e.target]=1;if(e.target===selected)related[e.source]=1;});}
    var tp={};Object.keys(toolGroups).forEach(function(o){var at=pos[o];if(at)toolGroups[o].forEach(function(t,i){if(!pos[t.id])pos[t.id]=toolPosition(at,i,toolGroups[o].length);tp[t.id]=pos[t.id];});});
    var edges=d.edges.map(function(e){var a=pos[e.source],b=pos[e.target];if(!a||!b)return"";var target=(s.nodes||[]).find(function(n){return n.id===e.target;}),emphasis=!historyMode&&target&&displayEntityState(target,s).key==="working",active=!historyMode&&e.signal&&e.signal.kind==="relationship"&&e.signal.phase==="started"&&e.status==="running",rel=!selected||e.source===selected||e.target===selected,path=edgePath(a,b);return'<path id="edge-'+esc(e.id)+'" class="live-edge" data-source="'+esc(e.source)+'" data-target="'+esc(e.target)+'" data-active="'+active+'" data-emphasis="'+!!emphasis+'" data-related="'+rel+'" d="'+path+'"></path>'+(active?'<circle class="live-flow-dot" r="2.5"><animateMotion dur="1.5s" repeatCount="indefinite" path="'+path+'"></animateMotion></circle>':"");}).join("");
    Object.keys(toolGroups).forEach(function(o){var a=pos[o];toolGroups[o].forEach(function(t){var b=tp[t.id];if(!a||!b)return;var path="M"+(a.x+28)+" "+a.y+" C"+(a.x+70)+" "+a.y+" "+(b.x-96)+" "+b.y+" "+(b.x-78)+" "+b.y,started=t.lastSignal?t.lastSignal.kind==="operation"&&t.lastSignal.phase==="started":t.lastAction==="tool.started",active=!historyMode&&started&&displayEntityState(t,s).key==="working",rel=!selected||selected===o||selected===t.id;edges+='<path class="live-edge live-tool-edge" data-source="'+esc(o)+'" data-target="'+esc(t.id)+'" data-active="'+active+'" data-related="'+rel+'" d="'+path+'"></path>'+(active?'<circle class="live-flow-dot" r="2"><animateMotion dur="1.1s" repeatCount="indefinite" path="'+path+'"></animateMotion></circle>':"");});});
    var nodes=d.nodes.map(function(n){var p=pos[n.id],sel=selected===n.id,rel=!selected||!!related[n.id],pin=!!state.pinned[(s?sessionKey(s):"")+"|"+n.id],view=displayEntityState(n,s),present=!historyMode&&n.kind==="session"&&presenceState(s)==="present",meta=present?(ago(s&&s.presence&&s.presence.lastObservedAt)||"present now"):activityContext(n,view),work=view.key==="working"?actionText(n.lastAction,n,n.status):view.key==="present"?"Process active":view.label,past=d.history[n.id]||0;return'<g class="live-node" tabindex="0" role="button" data-node="'+esc(n.id)+'" data-kind="'+esc(n.kind||"agent")+'" data-status="'+esc(view.key)+'" data-presence="'+(present?"present":"unknown")+'" data-selected="'+sel+'" data-related="'+rel+'" data-pinned="'+pin+'" aria-label="'+esc(label(n)+", "+kindName(n.kind)+", "+view.label)+'" transform="translate('+p.x+" "+p.y+')"><circle class="node-aura" r="42"></circle>'+kindShape(n.kind)+'<circle class="node-status" cx="25" cy="-22" r="4"></circle><text class="node-name" text-anchor="middle" y="48">'+esc(short(label(n),20))+'</text><text class="node-eyebrow" text-anchor="middle" y="63">'+esc(short((kindName(n.kind)+" · "+hostName(n)).toUpperCase(),26))+'</text><g class="node-work" transform="translate(34 -55)"><rect width="170" height="42" rx="20"></rect><circle cx="15" cy="21" r="4"></circle><text x="28" y="18">'+esc(short(work,25))+'</text><text class="node-meta" x="28" y="32">'+esc(short(meta,28))+'</text></g>'+(past?'<g class="node-history" transform="translate(34 82)"><rect width="145" height="30" rx="15"></rect><text x="13" y="20">'+past+' earlier operations</text></g>':"")+"</g>";}).join("");
    var tools=d.tools.map(function(t){var p=tp[t.id];if(!p)return"";var rel=!selected||selected===t.id||selected===d.owner[t.id],view=displayEntityState(t,s),pin=!!state.pinned[(s?sessionKey(s):"")+"|"+t.id],elapsed=Number.isFinite(t.durationMs)?(t.durationMs<1000?t.durationMs+" ms":(t.durationMs/1000).toFixed(1)+" s"):"";return'<g class="live-tool" tabindex="0" role="button" data-node="'+esc(t.id)+'" data-owner="'+esc(d.owner[t.id]||"")+'" data-kind="'+esc(t.kind||"tool")+'" data-status="'+esc(view.key)+'" data-related="'+rel+'" data-pinned="'+pin+'" aria-label="'+esc(label(t)+", "+kindName(t.kind)+", "+view.label)+'" transform="translate('+p.x+" "+p.y+')"><rect class="live-tool-halo" x="-92" y="-23" width="184" height="46" rx="23"></rect><rect class="live-tool-card" x="-86" y="-19" width="172" height="38" rx="19"></rect><path class="live-tool-icon" d="M-70-1L-64-7-58-1-64 5Z"></path><text class="tool-name" x="-50" y="-3">'+esc(short(label(t),22))+'</text><text class="tool-state" x="-50" y="12">'+esc(short(view.label+(elapsed?" · "+elapsed:""),27))+"</text></g>";}).join("");
    el("live-edges").innerHTML=edges;el("live-nodes").innerHTML=nodes;el("live-tools").innerHTML=tools;el("live-empty").hidden=d.nodes.length>0;el("live-graph-summary").textContent=d.nodes.length+" actors · "+d.tools.length+" shown of "+d.totalTools+" operations · "+d.edges.length+" relationships";applyCamera();renderSelection(s,d);renderGuidance(s,d);
  }
  function applyCamera(){el("live-viewport").setAttribute("transform","translate("+state.camera.x+" "+state.camera.y+") scale("+state.camera.k+")");el("live-zoom").textContent=Math.round(state.camera.k*100)+"%";}
  function setZoom(next,cx,cy){state.cameraEpoch++;var c=el("live-canvas"),r=c.getBoundingClientRect(),x=cx==null?r.width/2:cx-r.left,y=cy==null?r.height/2:cy-r.top,k=Math.max(.25,Math.min(2.8,next)),wx=(x-state.camera.x)/state.camera.k,wy=(y-state.camera.y)/state.camera.k;state.camera={x:x-wx*k,y:y-wy*k,k:k};applyCamera();}
  function fit(selector){var root=el(selector||"live-viewport"),box;try{box=root.getBBox();}catch(_){return;}if(!box.width&&!box.height)return;var c=el("live-canvas"),p=42,k=Math.max(.25,Math.min(2.5,Math.min((c.clientWidth-p*2)/Math.max(1,box.width),(c.clientHeight-p*2)/Math.max(1,box.height))));state.camera={k:k,x:(c.clientWidth-box.width*k)/2-box.x*k,y:(c.clientHeight-box.height*k)/2-box.y*k};applyCamera();}
  function fitSelection(){if(!state.node){fit();return;}var n=el("live-graph").querySelector('[data-node="'+CSS.escape(state.node)+'"]');if(!n)return;var b=n.getBBox(),c=el("live-canvas"),k=Math.min(2.2,Math.max(1,state.camera.k));state.camera={k:k,x:c.clientWidth/2-(b.x+b.width/2)*k,y:c.clientHeight/2-(b.y+b.height/2)*k};applyCamera();}
  function updateGeometry(id){var s=sessionById(state.selected),p=state.positions[s?sessionKey(s):"none"]||{},g=el("live-graph").querySelector('[data-node="'+CSS.escape(id)+'"]');if(g&&p[id])g.setAttribute("transform","translate("+p[id].x+" "+p[id].y+")");renderGraph(s);}
  function renderSelection(s,d){var value=(d.nodes.concat(d.tools)).find(function(n){return n.id===state.node;}),box=el("live-selection-body");if(!value){if(!s){box.textContent="Select a session, agent, or operation.";return;}var w=s.workspace,c=w&&w.changes,directory=usefulDirectory(w),workspaceFields=[["Execution host",hostName(s)],["Inference provider",inferenceProviderName(s)]];if(directory)workspaceFields.push(["Directory",directory]);workspaceFields.push(["Branch",w&&w.branchLabel?'<span class="live-detail-branch">'+branchIcon()+"<span>"+esc(w.branchLabel)+"</span></span>":"Not recorded",!!(w&&w.branchLabel)],[historicalView(s)?"Recorded":"Captured",w&&w.capturedAt?(ago(w.capturedAt)||w.capturedAt):"Not recorded"],["Source",w&&w.source||"Not recorded"],["Confidence",w&&w.confidence||"Not recorded"],["Tracked change counts",c?"+"+c.additions+" / −"+c.deletions+" lines across "+c.files+" files; not attributed to this session":"Not recorded"]);box.innerHTML="<strong>Working tree at capture</strong><dl>"+workspaceFields.map(function(x){return"<dt>"+esc(x[0])+"</dt><dd>"+(x[2]?x[1]:esc(x[1]))+"</dd>";}).join("")+"</dl>";return;}var view=displayEntityState(value,s),elapsed=Number.isFinite(value.durationMs)?(value.durationMs<1000?value.durationMs+" ms":(value.durationMs/1000).toFixed(1)+" s"):"Not available",owner=d.owner[value.id],ownerNode=d.nodes.find(function(n){return n.id===owner;}),fields=[["Type",kindName(value.kind)],["State",view.label],["Host",hostName(value)],["Provider",inferenceProviderName(value)],["Provider evidence",providerEvidence(value)],["Owner",ownerNode?label(ownerNode):"Not applicable"],["Elapsed",elapsed],[historicalView(s)?"Recorded state":"Current work",view.key==="working"?actionText(value.lastAction,value,value.status):view.label],["Evidence",value.confidence||"not reported"]];box.innerHTML="<strong>"+esc(label(value))+"</strong><dl>"+fields.map(function(x){return"<dt>"+esc(x[0])+"</dt><dd>"+esc(x[1])+"</dd>";}).join("")+"</dl>";}
  function renderGuidance(s,d){var c=coverageOf(s),value=(d.nodes.concat(d.tools)).find(function(n){return n.id===state.node;}),message;if(value){var view=displayEntityState(value,s);message="Selected "+label(value)+" · "+view.label+" · details opened in the evidence panel. Drag to pin; double-click to reset.";}else if(state.scope==="history"){message="Historical map · all nodes and relationships are static. Select an item for recorded evidence.";}else if(s&&c.activity==="presence-only"){message=hostName(s)+" process detected. Detailed activity, tools, transcript, and worker hierarchy are not available in Live mode for this session yet.";}else if(s&&c.activity==="unavailable"){message="Session evidence is retained, but detailed live activity is not reported by the current source.";}else{message="Select an actor or operation for details. Drag to pin · scroll to zoom · open Legend / Help for the visual key.";}el("live-map-guidance").textContent=message;}
  function tooltipValue(id){var s=sessionById(state.selected),d=graphData(s);return{session:s,data:d,value:d.nodes.concat(d.tools).find(function(n){return n.id===id;})};}
  function showTooltip(target){if(!target||!target.dataset.node)return;var found=tooltipValue(target.dataset.node),value=found.value;if(!value)return;var view=displayEntityState(value,found.session),owner=found.data.owner[value.id],ownerNode=found.data.nodes.find(function(n){return n.id===owner;}),tip=el("live-tooltip"),r=target.getBoundingClientRect();tip.innerHTML="<strong>"+esc(label(value))+"</strong>"+esc(kindName(value.kind)+" · "+view.label)+(ownerNode?"<br>Owned by "+esc(label(ownerNode)):"")+"<br>"+esc(hostName(value)+" · "+inferenceProviderName(value))+"<br>Evidence: "+esc(value.confidence||"not reported");tip.hidden=false;var w=Math.min(300,window.innerWidth-24),left=Math.max(12,Math.min(window.innerWidth-w-12,r.right+10)),top=Math.max(12,Math.min(window.innerHeight-tip.offsetHeight-12,r.top));tip.style.width=w+"px";tip.style.left=left+"px";tip.style.top=top+"px";target.setAttribute("aria-describedby","live-tooltip");}
  function hideTooltip(target){if(target&&document.activeElement===target)return;el("live-tooltip").hidden=true;if(target)target.removeAttribute("aria-describedby");}
  function showRowTooltip(target){if(!target)return;var tip=el("live-tooltip"),r=target.getBoundingClientRect(),heading=target.querySelector(".live-session-main span,.live-project span"),description=target.getAttribute("aria-label")||target.title||target.textContent;tip.innerHTML="<strong>"+esc(heading&&heading.textContent||"Session")+"</strong>"+esc(description);tip.hidden=false;var w=Math.min(300,window.innerWidth-24),left=Math.max(12,Math.min(window.innerWidth-w-12,r.right+10)),top=Math.max(12,Math.min(window.innerHeight-tip.offsetHeight-12,r.top));tip.style.width=w+"px";tip.style.left=left+"px";tip.style.top=top+"px";target.setAttribute("aria-describedby","live-tooltip");}
  function projectCatalog(){var snap=currentSnapshot(),given=Array.isArray(snap.projects)?snap.projects:[],by={};snap.sessions.forEach(function(s){if(!s.project||s.project==="unknown")return;var id=String(s.projectKey||projectName(s)),p=by[id]||(by[id]={id:id,name:projectName(s),sessionCount:0,liveCount:0,historicalCount:0,liveChildCount:0,historicalChildCount:0,updatedAt:s.updatedAt}),live=isLiveSession(s);if(isNavigationRoot(s)){p.sessionCount++;if(live)p.liveCount++;else p.historicalCount++;}else if(live)p.liveChildCount++;else p.historicalChildCount++;if(Date.parse(s.updatedAt||0)>Date.parse(p.updatedAt||0))p.updatedAt=s.updatedAt;});given.forEach(function(p){var name=p.label||p.name||p.project;if(!name||name==="unknown")return;var id=String(p.id||p.key||p.projectKey||name),derived=by[id]||{};if(Object.keys(derived).length)by[id]=Object.assign({},p,derived,{id:id,name:name||derived.name});});return Object.keys(by).map(function(k){return by[k];}).sort(function(a,b){return Number(b.liveCount||0)-Number(a.liveCount||0)||Date.parse(b.updatedAt||0)-Date.parse(a.updatedAt||0)||a.name.localeCompare(b.name);});}
  function duration(s){var start=s.startedAt||s.createdAt,end=s.endedAt||s.completedAt||((s.status==="completed"||s.status==="failed"||s.status==="cancelled")&&s.updatedAt),a=Date.parse(start||""),b=Date.parse(end||""),ms=b-a;if(!Number.isFinite(a)||!Number.isFinite(b)||ms<=0)return ago(s.updatedAt)||"activity time unavailable";var m=Math.max(1,Math.round(ms/60000));return m<60?m+"m":Math.floor(m/60)+"h "+m%60+"m";}
  function sessionMarkup(s,child){var live=state.scope==="live"&&isLiveSession(s),key=sessionKey(s),view=displaySessionState(s),identity=hostName(s)+" · "+inferenceProviderName(s),name=s.title||s.summary||(child?"Worker thread":hostName(s)+" session"),description=name+". "+view.label+". "+duration(s)+". "+workspaceDescription(s);return'<button class="live-session'+(child?" live-session-child":"")+'" type="button" data-session="'+esc(s.id)+'" data-key="'+esc(key)+'" data-live="'+live+'" data-state="'+esc(view.key)+'" aria-current="'+(key===state.selected)+'" aria-label="'+esc(description)+'" title="'+esc(description)+'"><span class="live-host" data-host="'+esc(hostOf(s))+'" aria-hidden="true">'+hostIcon(s)+'</span><span class="live-session-main"><span>'+esc(short(name,31))+'</span>'+workspaceDelta(s)+'</span><span class="live-session-identity"><i data-status="'+esc(view.key)+'"></i>'+esc(child?"Sub-agent · "+identity:identity)+" · "+esc(view.label)+" · "+esc(duration(s))+'</span>'+workspaceMarkup(s)+"</button>";}
  function workerMarkup(s,n){var view=displayEntityState(n,s),worker=n&&n.host?n:s,description=(label(n)||"Worker")+". Worker view. "+view.label+". "+workspaceDescription(s);return'<button class="live-session live-session-child" type="button" data-session="'+esc(s.id)+'" data-key="'+esc(sessionKey(s))+'" data-actor="'+esc(n.id)+'" data-live="'+(state.scope==="live"&&view.key==="working")+'" data-state="'+esc(view.key)+'" aria-current="'+(sessionKey(s)===state.selected&&state.node===n.id)+'" aria-label="'+esc(description)+'"><span class="live-host" data-host="'+esc(hostOf(worker))+'" aria-hidden="true">'+hostIcon(worker)+'</span><span class="live-session-main"><span>'+esc(short(label(n)||"Worker",31))+'</span></span><span class="live-session-identity"><i data-status="'+esc(view.key)+'"></i>Worker view · '+esc(hostName(worker))+" · "+esc(view.label)+" · "+esc(n.confidence||"evidence not reported")+'</span>'+workspaceMarkup(s)+"</button>";}
  function renderSessions(){var allProjects=projectCatalog(),projects=allProjects.filter(function(p){return Number(state.scope==="live"?p.liveCount:p.historicalCount)>0;}),list=visibleSessions(),current=projects.find(function(p){return p.id===state.project;}),selected=sessionById(state.selected),browser=el("live-browser"),scopedRoots=currentSnapshot().sessions.filter(function(s){return isNavigationRoot(s)&&(state.scope==="live"?isLiveSession(s):!isLiveSession(s));});if(!current){state.project=projects[0]&&projects[0].id||null;current=projects[0];list=visibleSessions();}browser.dataset.level=state.browserLevel;el("live-view-summary").textContent=state.scope==="live"?(scopedRoots.length?scopedRoots.length+" active session"+(scopedRoots.length===1?"":"s")+" across "+projects.length+" project"+(projects.length===1?"":"s"):"No live sessions across 0 projects"):(scopedRoots.length+" historical session"+(scopedRoots.length===1?"":"s")+" across "+projects.length+" project"+(projects.length===1?"":"s"));el("live-browser-kicker").textContent=state.browserLevel==="sessions"?"PROJECT":state.scope==="live"?"LIVE WORKSPACES":"HISTORY";el("live-browser-title").textContent=state.browserLevel==="sessions"&&current?short(current.name,26):"Projects";el("live-project-count").textContent=String(projects.length);el("live-project-list").innerHTML=projects.map(function(p){var sessions=Number(state.scope==="live"?p.liveCount:p.historicalCount),workers=Number(state.scope==="live"?p.liveChildCount:p.historicalChildCount),description=p.name+". "+sessions+" "+(state.scope==="live"?"live":"historical")+" sessions"+(workers?" and "+workers+" workers":"")+". "+(ago(p.updatedAt)||"Activity time unavailable");return'<button class="live-project" type="button" data-project="'+esc(p.id)+'" data-live="'+(state.scope==="live"&&sessions>0)+'" aria-current="'+(p.id===state.project)+'" aria-label="'+esc(description)+'"><span>'+esc(short(p.name,34))+"</span><small>"+(state.scope==="live"?'<b>'+sessions+' ACTIVE</b> · ':"")+sessions+" session"+(sessions===1?"":"s")+(workers?" · "+workers+" workers":"")+" · "+esc(ago(p.updatedAt)||"activity time unavailable")+"</small></button>";}).join("")||'<div class="live-browser-empty">'+(state.scope==="live"?"No projects have a live session.":"No retained project history found.")+"</div>";var selectedRoot=rootSession(selected);if(!selectedRoot||!list.some(function(s){return sessionKey(s)===sessionKey(selectedRoot);})){selectSession(list[0]&&sessionKey(list[0]),true);list=visibleSessions();selected=sessionById(state.selected);selectedRoot=rootSession(selected);}el("live-session-heading").textContent=current?short(current.name,36):"Sessions";el("live-count").textContent=String(list.length);el("live-session-list").innerHTML=list.map(function(s){var expanded=sessionKey(s)===sessionKey(selectedRoot),children=expanded?childSessions(s).filter(function(child){return state.scope==="live"?isLiveSession(child):!isLiveSession(child);}):[],workers=expanded?embeddedWorkers(s).filter(function(worker){var working=entityState(worker,s).key==="working";return state.scope==="live"?working:!working;}):[],nested=children.map(function(child){return sessionMarkup(child,true);}).concat(workers.map(function(worker){return workerMarkup(s,worker);}));return'<div class="live-session-family">'+sessionMarkup(s,false)+(nested.length?'<div class="live-session-children" role="group" aria-label="Worker views">'+nested.join("")+"</div>":"")+"</div>";}).join("")||'<div class="live-browser-empty">No '+(state.scope==="live"?"live":"historical")+' sessions for this project.</div>';el("live-session-context-identity").textContent=selected?hostName(selected)+" · "+inferenceProviderName(selected):"—";el("live-session-context-project").textContent=selected?projectName(selected):"Choose a session";var selectedState=displaySessionState(selected);el("live-session-context-status").textContent=selected?selectedState.label+" · "+duration(selected):"Waiting for local evidence";}
  function renderHealth(){var h=state.snapshot.health||{},bad=0;el("live-health").innerHTML=Object.keys(h).sort().map(function(n){var v=h[n]||{};if(v.status==="error")bad++;return'<span class="live-health-item" data-status="'+esc(v.status||"unknown")+'"><b>'+esc(n)+'</b> · '+esc(v.status||"unknown")+" · "+Number(v.files||0)+" files · "+Number(v.events||0)+" events · "+Number(v.errors||0)+" errors</span>";}).join("");el("live-health-toggle").textContent=bad?bad+" source issue"+(bad===1?"":"s"):"Sources";}
  function render(){el("panel-observability").dataset.scope=state.scope;document.querySelectorAll("[data-live-scope]").forEach(function(item){item.setAttribute("aria-selected",String(item.dataset.liveScope===state.scope));});var windowGroup=el("observability-window");if(windowGroup)windowGroup.hidden=state.scope!=="history";el("observability-title").textContent=state.scope==="live"?"Live agent activity":"Session history";el("live-sub").textContent=state.scope==="live"?"Follow current sessions, agents, tools, and supported evidence as work happens.":"Inspect retained, inert evidence and deterministic playback from completed work.";el("live-transcript-kicker").textContent=state.scope==="live"?"LIVE EVIDENCE":"SESSION EVIDENCE";el("live-transcript-title").textContent=state.scope==="live"?"Session stream":"Recorded session";el("live-transcript-search").placeholder=state.scope==="live"?"Search this stream…":"Search retained evidence…";el("live-transcript-list").setAttribute("aria-live",state.scope==="live"?"polite":"off");renderConnection();renderSessions();renderHealth();el("live-cursor").textContent=currentSnapshot().cursor||"";var source=sessionById(state.selected),s=state.playback.mode==="review"&&state.playback.session||source;renderGraph(s);renderPlayback();if(s&&state.fitFor!==sessionKey(s)){state.fitFor=sessionKey(s);(window.requestAnimationFrame||setTimeout)(function(){fit();});}}
  function closeTranscript(){if(state.transcript.source)state.transcript.source.close();state.transcript.source=null;state.transcript.status="idle";}
  function transcriptStatus(k,t){state.transcript.status=k;var e=el("live-transcript-state");e.dataset.state=k;e.textContent=t;}
  function transcriptItems(v){if(Array.isArray(v))return v;if(v&&v.snapshot&&Array.isArray(v.snapshot.events))return v.snapshot.events;if(v&&Array.isArray(v.events))return v.events;if(v&&Array.isArray(v.items))return v.items;if(v&&Array.isArray(v.turns))return v.turns;if(v&&v.item)return[v.item];if(v&&v.turn)return[v.turn];return v&&typeof v==="object"?[v]:[];}
  function normalizeTurn(v){var type=String(v.role||v.type||v.kind||"event").toLowerCase(),role=type.includes("thinking")||type.includes("reason")?"thinking":type.includes("error")?"error":type.includes("tool_result")||type.includes("tool-result")||type.includes("result")?"tool_result":type.includes("tool")||v.tool||v.command||v.patch||v.arguments||v.args?"tool":type.includes("user")?"user":type.includes("assistant")||type.includes("agent")?"assistant":"event",agent=String(v.agentId||v.agent&&v.agent.id||v.actorId||v.actor&&v.actor.id||""),parts=[];
    function part(name,value){if(value==null||value==="")return;var text=value;if(typeof value==="object"){try{text=JSON.stringify(value,null,2);}catch(_){text="[structured content]";}}text=String(text);if(parts.some(function(p){return p.text===text;}))return;parts.push({name:name,text:text});}
    part("",v.content??v.text??v.message??v.body??v.summary);part("Details",v.details);part("Reasoning",v.reasoning);part("Command",v.command);part("Arguments",v.arguments??v.args??v.input);part("Patch",v.patch??v.diff);part("Result",v.result??v.output);part("Error",v.error??v.errorMessage);
    var tool=v.tool||{},toolLabel=tool.name||tool.category,content=parts.map(function(p){return(p.name?p.name+"\\n":"")+p.text;}).join("\\n\\n");if(!content&&toolLabel)content=statusText(tool.status);return{id:String(v.id||v.eventId||tool.callId||[v.timestamp||v.observedAt||"",role,agent,content.slice(0,80)].join("|")),role:role,content:content,agent:agent,label:String(v.label||v.toolName||v.name||toolLabel||role),at:v.timestamp||v.observedAt||v.at||"",tool:tool};}
  function addTranscript(v,reset){if(reset){state.transcript.turns=[];state.transcript.seen={};}transcriptItems(v).forEach(function(raw){var t=normalizeTurn(raw);if(!t.content||state.transcript.seen[t.id])return;state.transcript.seen[t.id]=1;state.transcript.turns.push(t);});if(state.transcript.turns.length>MAX_TURNS)state.transcript.turns=state.transcript.turns.slice(-MAX_TURNS);renderTranscript(true);}
  function transcriptSupported(s){return!!s&&coverageOf(s).transcript!=="unavailable"&&!/^runtime-/.test(String(s.id||""));}
  function openTranscript(s){closeTranscript();state.transcript.session=s&&sessionKey(s);state.transcript.turns=[];state.transcript.seen={};state.transcript.unread=0;state.transcript.follow=true;renderTranscript();if(!transcriptSupported(s)){transcriptStatus("idle","Transcript not reported by this source");return;}if(!window.EventSource)return;var url="/api/live/transcripts/"+encodeURIComponent(s.host)+"/"+encodeURIComponent(s.id)+"/events",src=new EventSource(dashSseUrl(url));state.transcript.source=src;transcriptStatus("connecting","Connecting");src.onopen=function(){transcriptStatus("live","Live");};src.onmessage=function(e){try{addTranscript(JSON.parse(e.data));}catch(_){}};["init","reset","turn","message","delta"].forEach(function(k){src.addEventListener(k,function(e){try{var d=JSON.parse(e.data);addTranscript(d,k==="reset"||!!d.reset);}catch(_){}});});src.onerror=function(){transcriptStatus("error","Unavailable");};}
  function renderTranscript(arrived){var s=sessionById(state.selected),q=state.transcript.query.toLowerCase(),agent=state.node,items=state.transcript.turns.filter(function(t){return(!q||(t.content+" "+t.label).toLowerCase().includes(q))&&(!agent||!t.agent||t.agent===agent);}).slice().reverse(),list=el("live-transcript-list"),oldHeight=list.scrollHeight,oldTop=list.scrollTop,nearTop=oldTop<56,following=state.scope==="live"&&state.playback.mode==="live";el("live-transcript-context").textContent=s?(s.project&&s.project!=="unknown"?s.project:"Project not reported")+" · "+hostName(s)+" · "+inferenceProviderName(s):(state.scope==="live"?"Select a live session to follow":"Select a historical session to inspect");el("live-transcript-clear-filter").hidden=!agent;if(agent)el("live-transcript-clear-filter").textContent="All agents";if(!items.length)list.innerHTML='<div class="live-transcript-empty">'+esc(q?"No matching transcript activity":agent?"No transcript activity attributed to this selection yet.":state.scope==="live"?"Transcript content stays local and appears here as the selected session runs.":"Select a historical session to inspect its retained evidence.")+"</div>";else list.innerHTML=items.map(function(t){var fold=t.role==="thinking"||t.role==="tool"||t.role==="tool_result"||t.role==="error",body='<div class="live-turn-body">'+esc(t.content)+"</div>";return'<article class="live-turn" data-turn="'+esc(t.id)+'" data-role="'+esc(t.role)+'" data-agent="'+esc(t.agent)+'"><div class="live-turn-head"><span>'+esc(t.label||t.role)+'</span><time>'+esc(ago(t.at)||"now")+"</time></div>"+(fold?"<details><summary>Show "+esc(t.role.replace("_"," "))+"</summary>"+body+"</details>":body)+"</article>";}).join("");if(arrived&&following){if(state.transcript.follow||nearTop){list.scrollTop=0;state.transcript.unread=0;}else{list.scrollTop=oldTop+Math.max(0,list.scrollHeight-oldHeight);state.transcript.unread++;}}else if(!following)state.transcript.unread=0;el("live-transcript-new").hidden=!following||!state.transcript.unread;el("live-transcript-new").textContent=state.transcript.unread+" new activit"+(state.transcript.unread===1?"y":"ies")+" ↑";}
  function stopPlayback(){if(state.playback.timer)clearInterval(state.playback.timer);state.playback.timer=null;state.playback.playing=false;}
  function playbackRecords(){return state.playback.events.length?state.playback.events:state.playback.items;}
  function reviewSession(index){var source=sessionById(state.selected),events=state.playback.events.slice(0,index+1),nodes={},edges={};if(source)nodes[source.id]={id:source.id,kind:"session",host:source.host,status:"running",label:source.title};events.forEach(function(ev){var a=ev.actor||{},t=ev.target||{},tool=ev.tool||{},aid=a.id||source&&source.id,tid=t.id||tool.callId;if(aid){var priorActor=nodes[aid]||{};nodes[aid]=Object.assign({},priorActor,{id:aid,kind:a.kind||(a.role==="tool"?"tool":aid===source.id?"session":"agent"),role:a.role||priorActor.role,label:a.label||priorActor.label,host:source&&source.host,status:ev.status||priorActor.status||"running",observedAt:ev.at});}if(tid){var prior=nodes[tid]||{},nextLabel=t.label||tool.name||tool.category||prior.label;nodes[tid]=Object.assign({},prior,{id:tid,kind:tool.callId?"tool":t.kind||prior.kind||"agent",role:t.role||prior.role,label:nextLabel,toolName:tool.name||prior.toolName,status:tool.status||ev.status||prior.status||"running",durationMs:tool.durationMs||prior.durationMs,observedAt:ev.at});var eid=String(aid+"|"+(tool.callId||tid));edges[eid]={id:eid,source:aid,target:tid,action:ev.relation||ev.kind||"activity"};}});return Object.assign({},source||{},{nodes:Object.keys(nodes).map(function(k){return nodes[k];}),edges:Object.keys(edges).map(function(k){return edges[k];})});}
  function setReviewIndex(index){var p=state.playback,records=playbackRecords(),max=Math.max(0,records.length-1);p.index=Math.max(0,Math.min(max,Number(index)||0));p.session=reviewSession(p.index);var cutoff=records[p.index]&&(records[p.index].at||records[p.index].observedAt||records[p.index].timestamp),items=p.items.filter(function(item,i){var at=item.at||item.observedAt||item.timestamp;return cutoff&&at?Date.parse(at)<=Date.parse(cutoff):i<=Math.round(p.index/Math.max(1,records.length-1)*Math.max(0,p.items.length-1));});addTranscript(items.length?items:records.slice(0,p.index+1),true);renderGraph(p.session);renderPlayback();}
  function renderPlayback(){var p=state.playback,review=p.mode==="review",history=state.scope==="history",selected=sessionById(state.selected),active=isLiveSession(selected),supported=transcriptSupported(selected),records=playbackRecords(),range=el("live-playback-range"),mode=el("live-mode"),playback=el("live-playback"),presentation=history?"history":review?"review":"live",reviewAvailable=active&&supported;mode.dataset.mode=presentation;mode.textContent=presentation.toUpperCase();playback.dataset.mode=presentation;playback.hidden=presentation==="live"&&!reviewAvailable;range.disabled=!review||!records.length;range.max=String(Math.max(0,records.length-1));range.value=String(review?p.index:Math.max(0,records.length-1));el("live-playback-toggle").disabled=!review||!records.length;el("live-playback-toggle").innerHTML=p.playing?"&#9208;":"&#9654;";el("live-playback-toggle").setAttribute("aria-label",p.playing?"Pause session review":"Play session review");el("live-enter-review").hidden=history||review||!reviewAvailable;el("live-resume-live").hidden=history||!review||!active;el("live-transcript-follow").hidden=history||review;var at=records[p.index]&&(records[p.index].observedAt||records[p.index].timestamp||records[p.index].at),stamp=at?new Date(at).toLocaleTimeString([], {hour:"2-digit",minute:"2-digit",second:"2-digit"}):"";el("live-playback-time").textContent=review?(history&&stamp?"Recorded "+stamp:stamp||String(Math.min(p.index+1,records.length))+" / "+String(records.length)):"Live now";}
  function loadPlayback(s){closeTranscript();stopPlayback();state.playback={mode:"review",events:[],items:[],index:0,playing:false,speed:state.playback.speed||1,timer:null,startAt:null,endAt:null,session:null,truncated:false,gap:false};transcriptStatus("connecting","Loading review");renderPlayback();if(!transcriptSupported(s)){transcriptStatus("idle","Playback not reported by this source");return;}fetch("/api/live/playback/"+encodeURIComponent(s.host)+"/"+encodeURIComponent(s.id),{cache:"no-store",headers:{"x-dash-token":DASH_TOKEN}}).then(function(r){if(!r.ok)throw Error();return r.json();}).then(function(v){state.playback.events=Array.isArray(v.events)?v.events:[];state.playback.items=transcriptItems(v.transcript||v.items||[]);state.playback.startAt=v.startAt||null;state.playback.endAt=v.endAt||null;state.playback.truncated=!!v.truncated;state.playback.gap=!!v.gap;var notice=el("live-replay-notice");notice.hidden=!state.playback.truncated&&!state.playback.gap;notice.textContent=state.playback.gap?"Some source events are unavailable; this replay contains an evidence gap.":state.playback.truncated?"Replay begins at the earliest retained local evidence.":"";var records=playbackRecords();setReviewIndex(Math.max(0,records.length-1));transcriptStatus("review","Review");}).catch(function(){state.playback.items=[];state.playback.events=[];state.transcript.turns=[];renderTranscript();transcriptStatus("error","Review unavailable");renderPlayback();});}
  function resumeLive(){var s=sessionById(state.selected);stopPlayback();state.playback.mode="live";state.playback.events=[];state.playback.items=[];state.playback.session=null;if(isLiveSession(s))openTranscript(s);else if(s)loadPlayback(s);render();}
  function selectSession(id,open){var s=sessionById(id),key=s?sessionKey(s):id;if(state.selected===key&&state.transcript.session===key)return;stopPlayback();state.selected=key||null;state.node=null;state.fitFor=null;state.camera={x:24,y:24,k:1};if(open!==false){if(isLiveSession(s)){state.playback.mode="live";openTranscript(s);}else if(s)loadPlayback(s);}}
  function selectNode(id){state.node=id||null;render();renderTranscript();var details=el("live-selection");if(id)details.open=true;var found=id&&tooltipValue(id).value;el("live-interaction-status").textContent=found?label(found)+" selected. Details opened.":"Showing all actors.";}
  function setTranscriptCollapsed(collapsed,persist){collapsed=!!collapsed;state.transcriptCollapsed=collapsed;var workspace=el("live-workspace"),panel=el("live-transcript-panel"),body=el("live-transcript-body"),button=el("live-transcript-toggle"),expanded=!collapsed;if(workspace)workspace.dataset.transcriptCollapsed=String(collapsed);if(panel)panel.dataset.collapsed=String(collapsed);if(body)body.hidden=collapsed;if(button){button.setAttribute("aria-expanded",String(expanded));button.setAttribute("aria-label",expanded?"Collapse Session stream":"Expand Session stream");button.title=expanded?"Collapse Session stream and expand Agent activity":"Expand Session stream";}if(persist!==false){try{localStorage.setItem(TRANSCRIPT_COLLAPSE_KEY,String(collapsed));}catch(_){}}if(el("live-interaction-status"))el("live-interaction-status").textContent=collapsed?"Session stream collapsed. Agent activity has more room.":"Session stream expanded.";}
  function setScope(scope,sync){if(scope!=="live"&&scope!=="history")return false;if(scope===state.scope){render();return true;}closeTranscript();stopPlayback();state.scope=scope;state.browserLevel="projects";state.project=null;state.selected=null;state.node=null;state.transcript.follow=scope==="live";state.transcript.unread=0;state.playback.mode="live";if(scope==="history")fetchHistory();else render();if(sync!==false&&window.AKDashboardSyncHash)window.AKDashboardSyncHash();return true;}
  function bind(){
    setTranscriptCollapsed(state.transcriptCollapsed,false);el("live-transcript-toggle").addEventListener("click",function(){setTranscriptCollapsed(!state.transcriptCollapsed,true);});
    el("live-pause").addEventListener("click",function(){state.paused=!state.paused;this.setAttribute("aria-pressed",String(state.paused));this.innerHTML=state.paused?"&#9654; Resume":"&#9208; Pause";el("live-canvas").dataset.paused=String(state.paused);var svg=el("live-graph");if(state.paused&&svg.pauseAnimations)svg.pauseAnimations();else if(!state.paused&&svg.unpauseAnimations)svg.unpauseAnimations();if(!state.paused){var q=state.pending.slice();state.pending=[];if(state.overflow){state.overflow=false;connect();}else q.forEach(function(p){receive(p.kind,p.data);});}});
    el("live-health-toggle").addEventListener("click",function(){var h=el("live-health"),open=h.hidden;h.hidden=!open;this.setAttribute("aria-expanded",String(open));if(open){var r=this.getBoundingClientRect(),w=Math.min(360,window.innerWidth-24);h.style.width=w+"px";h.style.left=Math.max(12,Math.min(window.innerWidth-w-12,r.right-w))+"px";h.style.top=Math.min(window.innerHeight-h.offsetHeight-12,r.bottom+8)+"px";}});
    el("live-project-list").addEventListener("click",function(e){var b=e.target.closest("[data-project]");if(!b)return;state.project=b.dataset.project;state.browserLevel="sessions";var list=visibleSessions();selectSession(list[0]&&sessionKey(list[0]));render();(el("live-session-list").querySelector("[data-session]")||el("live-browser-back")).focus();});
    var scopeTabs=Array.from(document.querySelectorAll("[data-live-scope]"));function activateScope(button){if(button)setScope(button.dataset.liveScope,true);}scopeTabs.forEach(function(button){button.addEventListener("click",function(){activateScope(this);});});el("live-scope-tabs").addEventListener("keydown",function(e){if(!/^(ArrowLeft|ArrowRight|Home|End)$/.test(e.key))return;var current=Math.max(0,scopeTabs.indexOf(document.activeElement)),next=e.key==="Home"?0:e.key==="End"?scopeTabs.length-1:(current+(e.key==="ArrowRight"?1:-1)+scopeTabs.length)%scopeTabs.length;e.preventDefault();scopeTabs[next].focus();activateScope(scopeTabs[next]);});
    var windowChips=el("observability-window");if(windowChips)windowChips.addEventListener("click",function(e){var b=e.target.closest("[data-history-window]");if(!b)return;var token=b.dataset.historyWindow;if(token===state.historyWindow)return;state.historyWindow=token;var all=windowChips.querySelectorAll("[data-history-window]");for(var i=0;i<all.length;i++)all[i].classList.toggle("on",all[i]===b);if(state.scope==="history")fetchHistory();});
    el("live-browser-back").addEventListener("click",function(){state.browserLevel="projects";renderSessions();var selectedProject=el("live-project-list").querySelector('[data-project="'+CSS.escape(state.project||"")+'"]');if(selectedProject)selectedProject.focus();});
    el("live-session-list").addEventListener("click",function(e){var b=e.target.closest("[data-session]");if(!b)return;selectSession(b.dataset.key||b.dataset.session);if(b.dataset.actor)selectNode(b.dataset.actor);else{render();el("live-selection").open=true;}});
    [el("live-project-list"),el("live-session-list")].forEach(function(list){list.addEventListener("pointerover",function(e){var row=e.target.closest(".live-project,.live-session");if(row)showRowTooltip(row);});list.addEventListener("pointerout",function(e){var row=e.target.closest(".live-project,.live-session");if(row&&!row.contains(e.relatedTarget))hideTooltip(row);});list.addEventListener("focusin",function(e){var row=e.target.closest(".live-project,.live-session");if(row)showRowTooltip(row);});list.addEventListener("focusout",function(e){var row=e.target.closest(".live-project,.live-session");if(row)hideTooltip(row);});});
    el("live-playback-range").addEventListener("input",function(){stopPlayback();setReviewIndex(this.value);});
    el("live-playback-speed").addEventListener("change",function(){state.playback.speed=Number(this.value)||1;if(state.playback.playing){stopPlayback();el("live-playback-toggle").click();}});
    el("live-playback-toggle").addEventListener("click",function(){var p=state.playback,records=playbackRecords();if(p.mode!=="review"||!records.length)return;if(p.playing){stopPlayback();renderPlayback();return;}if(p.index>=records.length-1)setReviewIndex(0);p.playing=true;function advance(){if(!p.playing)return;if(p.index>=records.length-1){stopPlayback();renderPlayback();return;}var here=Date.parse(records[p.index]&&(records[p.index].at||records[p.index].observedAt||records[p.index].timestamp)||0),next=Date.parse(records[p.index+1]&&(records[p.index+1].at||records[p.index+1].observedAt||records[p.index+1].timestamp)||0),delay=Number.isFinite(here)&&Number.isFinite(next)?Math.max(90,Math.min(1800,(next-here)/p.speed)):Math.max(120,700/p.speed);p.timer=setTimeout(function(){setReviewIndex(p.index+1);advance();},delay);}advance();renderPlayback();});
    el("live-enter-review").addEventListener("click",function(){var s=sessionById(state.selected);if(s)loadPlayback(s);});
    el("live-resume-live").addEventListener("click",resumeLive);
    el("live-zoom-in").onclick=function(){setZoom(state.camera.k*1.2);};el("live-zoom-out").onclick=function(){setZoom(state.camera.k/1.2);};el("live-fit").onclick=function(){state.cameraEpoch++;fit();};el("live-fit-selection").onclick=fitSelection;el("live-reset-layout").onclick=function(){var s=sessionById(state.selected),key=s?sessionKey(s):"none",prefix=key+"|";delete state.positions[key];Object.keys(state.pinned).forEach(function(k){if(k.indexOf(prefix)===0)delete state.pinned[k];});renderGraph(s,true);fit();el("live-interaction-status").textContent="Automatic layout restored for this session.";};
    el("live-legend-toggle").onclick=function(){var box=el("live-legend"),open=box.hidden;box.hidden=!open;this.setAttribute("aria-expanded",String(open));if(open)el("live-legend-close").focus();};el("live-legend-close").onclick=function(){el("live-legend").hidden=true;el("live-legend-toggle").setAttribute("aria-expanded","false");el("live-legend-toggle").focus();};
    el("live-transcript-search").addEventListener("input",function(){state.transcript.query=this.value;renderTranscript();});el("live-transcript-list").addEventListener("scroll",function(){var near=this.scrollTop<56;if(!near&&state.transcript.follow){state.transcript.follow=false;el("live-transcript-follow").setAttribute("aria-pressed","false");el("live-transcript-follow").textContent="Follow";}});el("live-transcript-follow").onclick=function(){state.transcript.follow=!state.transcript.follow;this.setAttribute("aria-pressed",String(state.transcript.follow));this.textContent=state.transcript.follow?"Following":"Follow";if(state.transcript.follow){var l=el("live-transcript-list");l.scrollTop=0;state.transcript.unread=0;renderTranscript();}};el("live-transcript-clear-filter").onclick=function(){selectNode(null);};el("live-transcript-new").onclick=function(){state.transcript.follow=true;el("live-transcript-follow").setAttribute("aria-pressed","true");el("live-transcript-follow").textContent="Following";var l=el("live-transcript-list");l.scrollTop=0;state.transcript.unread=0;renderTranscript();};
    var canvas=el("live-canvas"),graph=el("live-graph");canvas.addEventListener("wheel",function(e){e.preventDefault();setZoom(state.camera.k*Math.exp(-e.deltaY*.0015),e.clientX,e.clientY);},{passive:false});graph.addEventListener("pointerover",function(e){var n=e.target.closest&&e.target.closest("[data-node]");if(n)showTooltip(n);});graph.addEventListener("pointerout",function(e){var n=e.target.closest&&e.target.closest("[data-node]");if(n&&!n.contains(e.relatedTarget))hideTooltip(n);});graph.addEventListener("focusin",function(e){var n=e.target.closest&&e.target.closest("[data-node]");if(n)showTooltip(n);});graph.addEventListener("focusout",function(e){var n=e.target.closest&&e.target.closest("[data-node]");if(n)hideTooltip(n);});
    canvas.addEventListener("pointerdown",function(e){if(e.target.closest&&e.target.closest("button,input,select,.live-legend"))return;var n=e.target.closest&&e.target.closest("[data-node]");state.cameraEpoch++;state.pointer={id:e.pointerId,x:e.clientX,y:e.clientY,sx:e.clientX,sy:e.clientY,moved:false,node:n&&n.dataset.node};canvas.setPointerCapture(e.pointerId);canvas.dataset.panning=String(!n);});
    canvas.addEventListener("pointermove",function(e){var p=state.pointer;if(!p||p.id!==e.pointerId)return;var dx=e.clientX-p.x,dy=e.clientY-p.y;if(Math.abs(e.clientX-p.sx)+Math.abs(e.clientY-p.sy)>4)p.moved=true;if(p.node){var s=sessionById(state.selected),key=s?sessionKey(s):"none",positions=state.positions[key],at=positions&&positions[p.node],mx=dx/state.camera.k,my=dy/state.camera.k;if(at){at.x+=mx;at.y+=my;var d=graphData(s);Object.keys(d.owner).forEach(function(tool){if(d.owner[tool]===p.node&&positions[tool]&&!state.pinned[key+"|"+tool]){positions[tool].x+=mx;positions[tool].y+=my;}});state.pinned[key+"|"+p.node]=true;updateGeometry(p.node);}}else{state.camera.x+=dx;state.camera.y+=dy;applyCamera();}p.x=e.clientX;p.y=e.clientY;});
    function up(e){var p=state.pointer;if(!p||p.id!==e.pointerId)return;state.pointer=null;canvas.removeAttribute("data-panning");if(p.node&&!p.moved)selectNode(p.node);}canvas.addEventListener("pointerup",up);canvas.addEventListener("pointercancel",up);
    graph.addEventListener("dblclick",function(e){var n=e.target.closest&&e.target.closest("[data-node]");if(!n)return;var s=sessionById(state.selected),key=s?sessionKey(s):"none",positions=state.positions[key]||{};delete state.pinned[key+"|"+n.dataset.node];delete positions[n.dataset.node];renderGraph(s);el("live-interaction-status").textContent="Automatic position restored for "+n.getAttribute("aria-label")+".";});
    graph.addEventListener("keydown",function(e){var n=e.target.closest&&e.target.closest("[data-node]");if(!n)return;if(e.key==="Enter"||e.key===" "){selectNode(n.dataset.node);e.preventDefault();}});
    canvas.addEventListener("keydown",function(e){if(e.target.closest&&e.target.closest("[data-node]"))return;var ok=true;if(e.key==="+"||e.key==="=")setZoom(state.camera.k*1.2);else if(e.key==="-"||e.key==="_")setZoom(state.camera.k/1.2);else if(e.key.toLowerCase()==="f")fit();else if(e.key==="0"){state.camera={x:24,y:24,k:1};applyCamera();}else ok=false;if(ok)e.preventDefault();});document.addEventListener("keydown",function(e){if(e.key!=="Escape")return;if(!el("live-health").hidden){el("live-health").hidden=true;el("live-health-toggle").setAttribute("aria-expanded","false");el("live-health-toggle").focus();}else if(!el("live-legend").hidden){el("live-legend").hidden=true;el("live-legend-toggle").setAttribute("aria-expanded","false");el("live-legend-toggle").focus();}});document.addEventListener("pointerdown",function(e){var h=el("live-health"),b=el("live-health-toggle");if(!h.hidden&&!h.contains(e.target)&&!b.contains(e.target)){h.hidden=true;b.setAttribute("aria-expanded","false");}var l=el("live-legend"),lb=el("live-legend-toggle");if(!l.hidden&&!l.contains(e.target)&&!lb.contains(e.target)){l.hidden=true;lb.setAttribute("aria-expanded","false");}});
  }
  function activate(){state.active=true;if(!state.source)connect();render();}function deactivate(){state.active=false;if(state.source){state.source.close();state.source=null;}stopPlayback();closeTranscript();setConnection("offline","Observability inactive");}
  bind();setInterval(function(){if(state.active&&!state.paused&&state.lastAt&&Date.now()-state.lastAt>30000)setConnection("stale","Connected · no new activity for 30 seconds");},5000);
  window.AKLive={activate:activate,deactivate:deactivate,setScope:setScope,setTranscriptCollapsed:setTranscriptCollapsed,render:render,receive:receive,state:state,fit:fit,fitSelection:fitSelection,selectNode:selectNode,addTranscript:addTranscript};
})();`;
